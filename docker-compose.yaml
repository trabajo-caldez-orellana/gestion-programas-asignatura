version: '3'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"  # Puerto del contenedor expuesto

    volumes:
      - ./:/app

volumes:
  backend_volume: